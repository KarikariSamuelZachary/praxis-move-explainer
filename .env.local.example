DATABASE_URL=postgresql://praxis_user:praxis123@localhost:5432/praxis
INTERNAL_SECRET=dev-secret
# Optional: pin the local engine to match production (the deploy image ships
# Stockfish 19 at /opt/stockfish/stockfish). Install it with:
#   python scripts/prewarm_stockfish.py --dest ~/.local/opt/stockfish
# STOCKFISH_PATH=/home/you/.local/opt/stockfish/stockfish